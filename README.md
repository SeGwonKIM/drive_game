# drive_game

Three.js 기반 3D 드라이빙 게임. 빌드 도구 없이 `index.html` 하나로 동작하며, 정적 호스팅(GitHub Pages 등)에 바로 배포할 수 있습니다.

## 플레이 방법

`index.html`을 브라우저로 열면 바로 실행됩니다.

- 차량 선택 → START
- 조작: `↑`/`W` 가속, `↓`/`S` 브레이크, `←`/`A`, `→`/`D` 조향
- 도로를 벗어나면 감속 페널티가 적용됩니다
- 3랩을 완주하면 총 기록과 베스트 랩이 표시되며, 최고 기록은 브라우저에 저장됩니다
